import { FC } from "react";
import { Image, View } from "react-native";

const Avatar: FC = () => {
    return (
        <View>
            <Image />
        </View>
    );
};

export default Avatar;
